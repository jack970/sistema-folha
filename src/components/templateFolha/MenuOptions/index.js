import React from 'react'
import * as S from './styled'

const MenuOptions = () => {

    return(
        <S.MenuOptions className="nav nav-tabs">
            <S.MenuList>
                <S.MenuLink href="#!" className="nav-link">Contra-Cheque</S.MenuLink>
            </S.MenuList>
        </S.MenuOptions>
    )
}

export default MenuOptions